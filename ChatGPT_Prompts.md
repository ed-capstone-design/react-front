# ChatGPT 질문용 프롬프트

## 🔥 짧은 버전 (즉시 해결용)
```
Spring Boot에서 SockJS WebSocket 연결 시 401 에러 발생. 
/ws/info 엔드포인트가 JWT 인증을 요구해서 연결 실패함. 
SecurityConfig에서 /ws/** 경로를 permitAll()로 설정하고, 
STOMP 인터셉터에서 실제 JWT 토큰 검증하는 방법 알려줘.
```

## 📋 상세 버전 (완전한 설정용)
```
React + Spring Boot 환경에서 WebSocket 연결 문제 해결 방법 질문:

1. 현재 상황:
- SockJS로 WebSocket 연결 시도
- /ws/info 엔드포인트에서 401 Unauthorized 에러
- JWT 토큰은 정상 존재하지만 SockJS 핸드셰이크에서 전달 안됨

2. 필요한 해결책:
- SecurityConfig에서 WebSocket 경로 인증 예외 처리
- STOMP 인터셉터에서 JWT 토큰 검증
- WebSocketConfig 설정

3. 기술 스택:
- Spring Boot + Spring Security + JWT
- React + SockJS + STOMP
- 실시간 알림 시스템

완전한 코드 예시와 설정 방법 알려줘.
```

## 🚀 코드 검토용
```
내 Spring Security WebSocket 설정 검토해줘:

현재 에러: GET /ws/info 401 Unauthorized
목표: SockJS 핸드셰이크는 인증 예외, STOMP 연결에서 JWT 검증

[여기에 현재 SecurityConfig, WebSocketConfig 코드 붙여넣기]

뭘 추가/수정해야 하는지 알려줘.
```

## 💡 사용 팁
1. **즉시 해결**이 필요하면 → 🔥 짧은 버전 사용
2. **완전한 구현**이 필요하면 → 📋 상세 버전 사용  
3. **기존 코드 검토**가 필요하면 → 🚀 코드 검토용 사용